;;; package --- Summary
;;; commentary:
;;; Code:
(require 'use-package)
(use-package tide
             :commands tide
             :bind (
                    )
             :config (
             ))

(provide 'typescriptsetup.el)
;;; typescriptsetup.el ends here
