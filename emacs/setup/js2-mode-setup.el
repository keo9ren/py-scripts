;;; package-- - Summary
;;; commentary:
;;; Code:

(require 'use-package)
(use-package js2-mode
  ;; (progn
  ;;   (add-to-list 'auto-mode-alist '("\\.js\\'" . js2-mode))
  ;;   (add-hook 'js2-mode-hook #'td)
  ;;   )
)

(provide 'setup.el)
;;; setup.el ends here
